{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "watch": "browser-sync start --config browser-sync.cjs"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "browser-sync": "^3.0.4",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
