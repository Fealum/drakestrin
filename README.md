# Drakestrin

## Local email

The development Docker Compose stack captures all SMTP mail with Mailpit. Its inbox is available at http://localhost:8025; SMTP remains internal at `mailpit:1025` and does not deliver messages externally.
